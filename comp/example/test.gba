const a = 2;
const b = 5;
const d = true;
const c = a + b;
const f = [1, 2, 3, 4];

fn g(): u32 {
    12
}

const h = g();

comp fn j<out len: u16>(): [u8; len] {
    len = 2;
    [0, 1]
}

const k = j();

struct Pos {
    x: u8,
    y: u8,
}

impl Pos {
    fn x(&this): u8 {
        this.x
    }
}

struct A {
    foo: ptr,
}

const l = Pos { x: 255, y: 127 };
const m = l.x();
