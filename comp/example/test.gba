const a = 2;
const b = 5;
const d = true;
const c = a + b;
const f = [1, 2, 3, 4];

fn g(): u32 {
    12
}

const h = g();

comp fn j<out len: u16>(): [u8; len] {
    len = 2;
    [0, 1]
}

const k = j();

struct Pos {
    x: u32,
    y: u32,
}

struct A {
    foo: ptr,
}

// const l = Pos { x: 5, y: 3 };
