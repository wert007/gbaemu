const a = 2;
const b = 5;
const d = true;
const c = a + b;
const f = [1, 2, 3, 4];

fn g(): u32 {
    12
}

const h = g();

comp fn j<out len: u16>(): [u8; len] {
    len = 2;
    [0, 1]
}

const k = j();

fn n(a: u8): u8 {
    a * 2
}

const o = n(2);

struct Pos {
    x: u8,
    y: u8,
}

impl Pos {
    fn k(&this): u8 {
        this.y
    }
}

struct A {
    foo: ptr,
}

const l = Pos { x: 255, y: 127 };
const m = l.k();
const p = l.k;
const q = p();

enum BinaryResult {
    Success,
    Error,
}

impl BinaryResult {
    fn is_success(this): bool {
        match this {
            BinaryResult::Success => true,
            BinaryResult::Error => false,
        }
    }
}

const r = BinaryResult::Success;
const s = r.is_success();
const t = match true {
    true => 1,
    _ => {
        2
    },
};

fn id<T>(t: T): T {
    t
}

const u = id(42);
